# weather-data
Simple weather script that extracts data and inserts into database implementing retries and exception handling.
Uses a cron to execute the python script then inserts into a local host postgres db.
This can then be displayed on a localhost grafana dashboard.
